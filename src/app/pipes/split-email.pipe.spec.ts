import { SplitEmailPipe } from './split-email.pipe';

describe('SplitEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
